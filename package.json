{
  "name": "create-utils",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "release": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.8.1",
    "tslint": "^5.12.0",
    "tslint-config-standard": "^8.0.1"
  }
}
